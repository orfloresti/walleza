"""Placeholder smoke test for the PR1 scaffold.

Phase 2/3 replace this with real unit and integration tests (config, db,
security, auth flows). It exists only so `uv run pytest -q` exits cleanly
on a fresh clone before any application code lands.
"""


def test_scaffold_is_testable() -> None:
    assert True
